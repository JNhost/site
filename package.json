{
  "name": "jnhost-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "prebuild": "npm run clean:all",
    "build": "next build && npm run build:standalone",
    "build:standalone": "cp -r .next/static .next/standalone/.next/ && cp -r public .next/standalone/",
    "postbuild": "node scripts/generate-build-id.js",
    "build:analyze": "ANALYZE=true npm run build",
    "start": "next start",
    "start:prod": "node server.js",
    "lint": "next lint --fix",
    "lint:check": "next lint",
    "clean": "rm -rf .next out",
    "clean:cache": "rm -rf .next/cache",
    "clean:all": "rm -rf .next out node_modules/.cache"
  },
  "dependencies": {
    "next": "^16.2.6",
    "nodemailer": "^8.0.7",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "sanitize-html": "^2.17.4",
    "yup": "^1.7.1"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.0",
    "@types/node": "^25.8.0",
    "@types/nodemailer": "^8.0.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "tailwindcss": "^4.3.0",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "postcss": "^8.5.10"
  }
}
